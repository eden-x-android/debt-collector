// Public API (server) của entity record — dùng trong route handlers.
export {
  addRecord,
  addRecordSchema,
  deleteRecord,
  listHistory,
  markRecordDone,
  restoreRecord,
} from "./record-service";
